pipeline {
    agent
        docker true
        label 'docker'

    }
    stages {
        stage('Run Tests') {
            steps {
                sh '''
                    echo 'Starting Playwright tests...'
                    npx playwright test'
                '''
                 }
        }
      }
