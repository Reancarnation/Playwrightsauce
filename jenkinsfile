pipeline {
    agent
        any

    }
    stages {
        stage('Run Tests') {
            steps {
                sh '''
                    echo 'Starting Playwright tests...'
                    npx playwright test'
                '''
                 }
        }
      }
